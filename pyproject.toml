[project]
name = "penpal-app"
version = "0.1.0"
description = "A streamlit app to display a gallery from the penpal creative coding project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cairosvg>=2.9.0",
    "streamlit>=1.54.0",
]
